server:
  address: localhost
  port: 8080

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/klh?serverTimezone=UTC&characterEncoding=UTF-8
    username: root
    password: root